# Extra dependencies for Contact/web_convert (web-based data conversion UI)
# Install into the same `contact` conda env:
#   pip install -r requirements.txt -r requirements_web_convert.txt

open3d>=0.19.0
plotly>=5.17.0
flask-socketio>=5.3.0
python-socketio>=5.9.0

# Optional: if you want faster async server modes
# eventlet
# gevent
